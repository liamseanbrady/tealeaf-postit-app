<%= render 'shared/content_header', title: 'Create a New Category' %>

<%= render 'shared/errors', ar_obj: @category %>

<%= form_for @category do |f|%>
  <%= f.text_field :name %>
  <br>
  <%= f.submit 'Create Category', class: 'btn btn-primary' %>
<% end %>